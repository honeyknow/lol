# API Client

Typed frontend API client.

`client.ts` centralizes Axios setup, request typing, and backend route wrappers. The client uses `/api` as its base path; Vite proxies that path to FastAPI during development.

